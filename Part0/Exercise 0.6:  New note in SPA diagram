sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save button
    Note right of browser: Browser executes event handler that creates the note object, adds it to notes list, and rerenders the page

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of browser: Browser sends POST request with JSON data: { content: " ", date: " " }
    server-->>browser: HTTP status code 201 (Created)
    deactivate server

    Note right of browser: Browser stays on the same page and no further HTTP requests are made
